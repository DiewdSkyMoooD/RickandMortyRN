import { View, ActivityIndicator } from "react-native";
import React from "react";

const RenderLoader = () => {
  return <ActivityIndicator size="large" color="#aaa"></ActivityIndicator>;
};

export default RenderLoader;
